# Restaurant Management System - Environment Configuration
# Copy this file to .env and fill in your values

# API Configuration
VITE_API_BASE_URL=https://your-backend-url.com/api
VITE_APP_NAME=Restoran Yönetim Sistemi

# Security Configuration (REQUIRED FOR PRODUCTION)
# Generate a secure random 32+ character key for production
# You can use: openssl rand -base64 32
VITE_CLIENT_ENCRYPTION_KEY=your-secure-32-character-encryption-key-here

# Frontend URL (for password reset emails)
VITE_FRONTEND_URL=https://your-frontend-url.com

# Debug Settings (SET TO false IN PRODUCTION)
VITE_DEBUG_AUTH=false
VITE_DEBUG_STORAGE=false
VITE_DEBUG_HTTP=false
VITE_DEBUG_SERVICES=false

# Production Security Notes:
# 1. NEVER commit your actual .env file to version control
# 2. Use HTTPS in production for VITE_API_BASE_URL
# 3. Generate a unique encryption key for each environment
# 4. Disable all debug flags in production
# 5. Consider using HttpOnly cookies for authentication tokens
